<h1>Welcome to Wikiful</h1>
  <h5><font color= #f0ad4e>A Thinkful Product</font></h5>

<div class="page-header">
  
</div>



  
  <body>
    <% @recent_articles.each do |article| %>

       	 	            <div class="well well-small">

        <h4><%= link_to article.title, article %></h4>

      
	 <p><%= article.created_at.strftime('%b %d, %Y') %></P> 


 		<h5><%= article.categories.map { |category| link_to category.name, category_path(category.id)}.join(', ').html_safe %> </h5>

        <p><%= truncate(article.content, length: 300) %></p>
       <%= link_to "View Full Article", article %>

  
</div>
      
    <% end %>
  </body>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_article_path,
            :class => 'btn btn-primary' %>
       